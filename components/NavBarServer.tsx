// Pure server component placeholder — no client imports, no next/link, no hooks.
// NavBarClient loads via client-side dynamic import in the actual page render.
export default function NavBarServer() {
  return <div id="nav-placeholder" />;
}
